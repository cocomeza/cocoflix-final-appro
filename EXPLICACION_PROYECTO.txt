PROYECTO FINAL: Cocoflix (Lista de Películas)

---

¿De qué trata Cocoflix?
----------------------
Cocoflix es una aplicación web donde podés anotar películas que querés ver, marcarlas como vistas y filtrarlas según el estado o buscarlas por nombre/género. También incluye una galería visual de estrenos 2026. Todo se guarda en el navegador, así que no se pierde al recargar la página.

---

¿Cómo hice el proyecto? (Explicación paso a paso para principiantes)
-------------------------------------------------------------------

1. **Creación del proyecto**
   - Usé Vite para crear un proyecto React porque es rápido y fácil de usar.
   - Vite es una herramienta que te ayuda a crear y desarrollar aplicaciones web modernas.
   - Instalé las dependencias con `npm install` (npm es el gestor de paquetes de Node.js).

2. **Estructura de carpetas y archivos (Modularización)**
   - Organicé el código en la carpeta `src/components` para los componentes reutilizables.
   - Cada componente tiene su propio archivo (.jsx) y algunos tienen su propio archivo de estilos (.css).
   - Los archivos principales son: `App.jsx`, `App.css`, y los componentes en `components/`.
   - Las imágenes se guardan en `public/img/` para acceso directo.

3. **Componentes principales (Explicación detallada)**

   **App.jsx (Componente Padre):**
   - Es el corazón de la app. Maneja el estado global (datos que se comparten entre componentes).
   - Controla qué página se muestra: "Inicio" o "Estrenos 2026".
   - Usa `useState` para manejar `paginaActual` ("home" o "estrenos").
   - Pasa funciones y datos a los componentes hijos mediante props.

   **Header.jsx:**
   - Muestra el título "Cocoflix 🎬" de la aplicación.
   - Tiene su propio archivo de estilos `Header.css`.
   - Es un componente simple que solo muestra información.

   **Nav.jsx (Navegación):**
   - Barra de navegación con botones "Inicio" y "Estrenos 2026".
   - Recibe funciones como props para cambiar la página actual.
   - El botón activo se resalta visualmente.
   - No usa React Router, sino estado local para simular navegación.

   **Home.jsx (Página Principal):**
   - Contiene toda la lógica de la lista de películas.
   - Maneja el estado de las películas, filtros y localStorage.
   - Incluye el formulario, filtros y la lista de películas.
   - Usa `useState` para: `peliculas`, `filtroTexto`, `filtroEstado`.
   - Usa `useEffect` para guardar/cargar datos del localStorage.

   **Estrenos2026.jsx (Galería de Estrenos):**
   - Muestra una galería visual de películas de estreno 2026.
   - Puedes agregar imágenes locales (guardadas en public/img/) o externas (URLs).
   - Usa CSS Grid para mostrar las imágenes en filas de 3 (responsivo).
   - Ejemplo de cómo agregar imágenes:
     ```js
     // Para imágenes locales (guardadas en public/img/)
     const estrenos2026 = [
       {
         titulo: "Los Vengadores: Doomsday",
         imagen: "/img/avengers-doomsday.webp"  // Ruta desde public
       },
       {
         titulo: "Película Externa",
         imagen: "https://ejemplo.com/imagen.jpg"  // URL directa
       }
     ];
     ```

   **Footer.jsx:**
   - Pie de página simple con copyright.
   - Tiene su propio archivo de estilos `Footer.css`.

   **PeliculaForm.jsx:**
   - Formulario para agregar nuevas películas.
   - Es un "formulario controlado": cada input tiene su propio estado.
   - Valida que los campos no estén vacíos y que el año sea válido.
   - Al enviar, llama a la función `onAgregarPelicula` que viene por props.

   **FiltroPeliculas.jsx:**
   - Permite buscar películas por texto (título o género).
   - Permite filtrar por estado (todos, por ver, vistas).
   - Los filtros se aplican en tiempo real.
   - Usa props para recibir y actualizar el estado de los filtros.

   **ListaPeliculas.jsx:**
   - Muestra la lista de películas filtradas.
   - Por cada película, renderiza un componente `PeliculaItem`.
   - Usa `.map()` para convertir el array de películas en componentes.

   **PeliculaItem.jsx:**
   - Muestra cada película individual.
   - Incluye botones para eliminar o marcar como vista/por ver.
   - El estilo cambia según el estado (vista/por ver).

4. **Conceptos de React explicados**

   **Componentes:**
   - Son como "bloques de construcción" reutilizables.
   - Cada componente tiene su propia lógica y estilos.
   - Se pueden combinar para crear aplicaciones complejas.

   **Props (Propiedades):**
   - Son datos que se pasan de un componente padre a un hijo.
   - Ejemplo: `<Home peliculas={listaPeliculas} />`
   - Permiten que los componentes se comuniquen entre sí.

   **useState (Estado):**
   - Permite que un componente "recuerde" datos que pueden cambiar.
   - Ejemplo: `const [peliculas, setPeliculas] = useState([]);`
   - `peliculas` es el valor actual, `setPeliculas` es la función para cambiarlo.

   **useEffect (Efectos):**
   - Permite ejecutar código cuando algo cambia.
   - Ejemplo: guardar datos en localStorage cuando cambia la lista de películas.
   - Se ejecuta después de que el componente se renderiza.

   **Eventos:**
   - `onClick`: cuando se hace clic en algo.
   - `onChange`: cuando cambia el valor de un input.
   - `onSubmit`: cuando se envía un formulario.

5. **Manejo del estado y datos (Explicación detallada)**

   **Estado Global vs Local:**
   - Estado global: datos que se comparten entre varios componentes (ej: lista de películas).
   - Estado local: datos que solo usa un componente (ej: texto de un input).

   **localStorage:**
   - Es como una "memoria" del navegador que persiste entre recargas.
   - Solo guarda strings, por eso usamos `JSON.stringify()` y `JSON.parse()`.
   - Ejemplo:
     ```js
     // Guardar
     localStorage.setItem("peliculas", JSON.stringify(peliculas));
     
     // Cargar
     const peliculasGuardadas = localStorage.getItem("peliculas");
     if (peliculasGuardadas) {
       setPeliculas(JSON.parse(peliculasGuardadas));
     }
     ```

6. **Formulario controlado (Explicación paso a paso)**

   ```js
   // 1. Estado para cada campo del formulario
   const [titulo, setTitulo] = useState("");
   const [genero, setGenero] = useState("");
   const [anio, setAnio] = useState("");

   // 2. Función que se ejecuta cuando cambia el input
   const handleTituloChange = (e) => {
     setTitulo(e.target.value); // e.target.value es el texto del input
   };

   // 3. Input controlado (el valor viene del estado)
   <input
     type="text"
     value={titulo}
     onChange={handleTituloChange}
     placeholder="Título de la película"
   />
   ```

7. **Renderizado y filtrado de la lista (Explicación detallada)**

   **Renderizado con .map():**
   ```js
   // Convierte cada película en un componente
   {peliculas.map((pelicula, index) => (
     <PeliculaItem
       key={index}
       pelicula={pelicula}
       onEliminar={() => eliminarPelicula(index)}
       onMarcarVista={() => marcarVista(index)}
     />
   ))}
   ```

   **Filtrado con .filter():**
   ```js
   // Filtra películas que coincidan con el texto de búsqueda
   const peliculasFiltradas = peliculas.filter((pelicula) => {
     const coincideTexto = pelicula.titulo.toLowerCase().includes(filtroTexto.toLowerCase());
     const coincideEstado = filtroEstado === "todos" || pelicula.estado === filtroEstado;
     return coincideTexto && coincideEstado;
   });
   ```

8. **Navegación por estado (Sin React Router)**

   ```js
   // En App.jsx
   const [paginaActual, setPaginaActual] = useState("home");

   // Renderizado condicional
   {paginaActual === "estrenos" ? <Estrenos2026 /> : <Home />}

   // En Nav.jsx
   <button onClick={() => setPaginaActual("home")}>Inicio</button>
   <button onClick={() => setPaginaActual("estrenos")}>Estrenos 2026</button>
   ```

9. **Estilos y diseño (CSS moderno)**

   **CSS Grid para la galería:**
   ```css
   .galeria-estrenos {
     display: grid;
     grid-template-columns: repeat(3, 1fr); /* 3 columnas */
     gap: 24px; /* espacio entre elementos */
   }
   
   /* Responsivo */
   @media (max-width: 900px) {
     grid-template-columns: repeat(2, 1fr); /* 2 columnas en tablets */
   }
   @media (max-width: 600px) {
     grid-template-columns: 1fr; /* 1 columna en móviles */
   }
   ```

   **Flexbox para layouts:**
   ```css
   .app-layout {
     display: flex;
     gap: 2rem;
   }
   ```

10. **Flujo de datos en la aplicación**

    ```
    App.jsx (Estado global)
         ↓ (props)
    Home.jsx (Lógica de películas)
         ↓ (props)
    PeliculaForm.jsx (Agregar películas)
    FiltroPeliculas.jsx (Filtrar películas)
    ListaPeliculas.jsx (Mostrar películas)
         ↓ (props)
    PeliculaItem.jsx (Película individual)
    ```

11. **Extras y buenas prácticas**

    **Archivos importantes:**
    - `.gitignore`: Lista archivos que no se suben al repositorio.
    - `package.json`: Configuración del proyecto y dependencias.
    - `README.md`: Documentación del proyecto.
    - `EXPLICACION_PROYECTO.txt`: Este archivo con explicaciones detalladas.

    **Estructura de carpetas recomendada:**
    ```
    public/
    └── img/              # Imágenes locales (acceso directo)
    src/
    ├── components/       # Componentes reutilizables
    ├── App.jsx          # Componente principal
    ├── App.css          # Estilos globales
    └── main.jsx         # Punto de entrada
    ```

    **Comandos útiles:**
    ```bash
    npm install          # Instalar dependencias
    npm run dev          # Iniciar servidor de desarrollo
    npm run build        # Construir para producción
    ```

---

¿Qué aprendí con este proyecto?
-------------------------------
- Cómo crear componentes reutilizables en React
- Cómo manejar estado local y global
- Cómo pasar datos entre componentes con props
- Cómo usar useEffect para efectos secundarios
- Cómo crear formularios controlados
- Cómo filtrar y renderizar listas
- Cómo usar localStorage para persistencia
- Cómo crear navegación sin React Router
- Cómo trabajar con imágenes locales (desde public/) y externas
- Cómo crear diseños responsivos con CSS Grid y Flexbox
- Cómo modularizar y organizar código en React

---

¿Cómo puedo mejorar este proyecto?
----------------------------------
- Agregar más páginas/secciones
- Implementar React Router para navegación real
- Agregar animaciones y transiciones
- Implementar un backend para guardar datos en una base de datos
- Agregar autenticación de usuarios
- Agregar más filtros y búsquedas avanzadas
- Implementar un sistema de calificaciones
- Agregar trailers o información detallada de las películas
- Crear una versión móvil con React Native
- Agregar tests unitarios

---

